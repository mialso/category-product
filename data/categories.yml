categories: !!seq
    - !!map { id: category_1, name: outer, ? parent, op: CREATE }
    - !!map { id: category_2, name: inner, ? parent, op: CREATE }
    - !!map { id: category_3, name: one, parent: category_1, op: CREATE }
    - !!map { id: category_4, name: two, parent: category_1, op: CREATE }
    - !!map { id: category_2, name: innerrr, ? parent, op: UPDATE }
    - !!map { id: category_1, name: outeeer, ? parent, op: UPDATE }
